import { SafeAreaView } from "react-native";
import React from "react";
import TerminologyScreen from "@/src/screens/terminology/screen";

const Index = () => {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <TerminologyScreen/>
    </SafeAreaView>
  );
};

export default Index;
