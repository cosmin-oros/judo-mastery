import { SafeAreaView } from "react-native";
import React from "react";
import DojosScreen from "@/src/screens/dojos/screen";

const Index = () => {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <DojosScreen/>
    </SafeAreaView>
  );
};

export default Index;
